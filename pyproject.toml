[project]
name = "ai-agent-test"
version = "0.2.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "langgraph",
    "numpy",
    "pandas",
    "pydantic",
    "python-dotenv",
    "requests",
    "uvicorn",
]

[tool.pyright]
pythonVersion = "3.11"
extraPaths = ["."]
